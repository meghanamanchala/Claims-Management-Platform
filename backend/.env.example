# Backend Environment Variables Template
PORT=5000

# MongoDB Atlas Connection String Template (Replace with your credentials in .env)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/claims_db?retryWrites=true&w=majority
